service {
  name = "web",
  port = 80,
  #address = "{{ external_gateway_ip }}", # This can be reused on different nodes, should be setup to a floating IP
  check {
    args = ["curl", "localhost"],
    interval = "3s"
  },
  tags = ["some_tag"]
}